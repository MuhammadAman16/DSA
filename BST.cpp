using namespace std;
#include <iostream>

class node
{
    int value;
    node *leftnode, *rightnode;
};
node *root = NULL;
int top = -1;
void BST()
{
    int array[10] = [ 25, 46, 7, 8, 1, 12, 10, 40, 52, 53 ];
    root->value = array[0];
    if (root->leftnode != NULL)
    {
        if (leftno)
        {
            /* code */
        }
        };
};